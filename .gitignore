# 1. Các thư viện Node.js (Tuyệt đối phải chặn vì nó rất nặng)
node_modules/

# 2. Biến môi trường & Bảo mật (Cực kỳ quan trọng, tránh lộ mật khẩu Database)
.env
.env.local
.env.development
.env.test
.env.production
*.pem

# 3. Log files (File rác sinh ra trong quá trình chạy)
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pids
*.pid
*.seed
*.pid.lock

# 4. Các file rác của hệ điều hành
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db

# 5. Thư mục cấu hình của IDE/Editor (VS Code, WebStorm...)
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?